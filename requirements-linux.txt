# Linux package. Core (edge TTS + audio decode) works headless; the tray
# shell needs a desktop with an X display (and the python3-tk system
# package, see install_linux.sh).
edge-tts>=6.1.12
miniaudio>=1.63
pystray>=0.19
pillow>=10.0
python-xlib>=0.33
